import React from 'react'
import styles from "@/styles/Home.module.scss";
import { Button } from '@chakra-ui/react'

export default function TagEarn() {
    return (
        <Button id={styles.tagEarn}>
            #EARN
        </Button>
    )
}
